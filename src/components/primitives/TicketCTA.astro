<style>
  .ticket-cta {
    position: relative;
    overflow: hidden;
  }

  .ticket-text {
    display: inline-block;
    position: relative;
    z-index: 1;
    transition: all 0.1s ease;
  }

  .ticket-arrow {
    position: absolute;
    right: 1.5rem;
    top: calc(50% - 1.5px);
    transform: translate(-0.5rem, -50%);
    z-index: 0;
    opacity: 0;
    transition: all 0.1s ease;
  }

  .ticket-cta:hover .ticket-arrow {
    transform: translate(0.25rem, -50%);
    opacity: 1;
  }

  .ticket-cta:hover .ticket-text {
    transform: translate(-0.5rem, 0);
    text-shadow: none;
  }
</style>

<a
  href="https://luma.com/ibeqegrr"
  class="ticket-cta bg-white text-black px-8 py-3 text-md font-bold font-mono uppercase hover:bg-gray-200 transition-colors border border-white inline-block"
  target="_blank"
  rel="noopener noreferrer"
>
  <span class="ticket-text">Buy Tickets</span>
  <span class="ticket-arrow" aria-hidden="true">→</span>
</a>
